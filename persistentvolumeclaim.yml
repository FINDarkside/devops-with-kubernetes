apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-claim
  namespace: main-app
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi